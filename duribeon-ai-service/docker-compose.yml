services:
  backend:
    build: ./apps/backend
    env_file: ./apps/backend/.env
    ports:
      - "8080:8080"
  frontend:
    build: ./apps/frontend
    environment:
      VITE_API_BASE_URL: http://localhost:8080
    ports:
      - "5173:80"
    depends_on:
      - backend
